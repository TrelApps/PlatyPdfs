<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PlatyPdfs.App">

    <!--<Style TargetType="local2:DialogCloseButton" xmlns:local2="using:PlatyPdfs.App.Interface">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local2:DialogCloseButton">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local2:LocalIcon" xmlns:local2="using:PlatyPdfs.App.Interface.Widgets">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local2:LocalIcon">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
</ResourceDictionary>
